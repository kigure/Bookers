<% if Book.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <% Book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>